#!/usr/bin/env node
// Generated by CoffeeScript 1.9.0
var argv, fs, module_root, path, realdir, realpath;

argv = require('yargs').argv;

path = require('path');

fs = require('fs');

realpath = fs.realpathSync(__filename);

realdir = path.dirname(realpath);

module_root = path.join(realdir, '..');

if (!argv["in"]) {
  throw new Error("No input file given");
}

if (!argv.out) {
  throw new Error("No output file given");
}

require(path.join(module_root, 'app'))(argv["in"], argv.out);
